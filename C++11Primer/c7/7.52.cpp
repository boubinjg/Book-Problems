There should not be a problem initializing the original version of Sales_data with those parameters
given the original version looks like this:

Sales_data {
	std::string ISBN;
	unsigned units_sold;
	double revenue;
};
