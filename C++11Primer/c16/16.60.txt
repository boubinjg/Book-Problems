makeshared constructs an object in dynamic memory as a shared_ptr. It takes arguments used to call the
constructor of the specified template object it is creating, then dynamically allocates that template
object as a shared_ptr.
