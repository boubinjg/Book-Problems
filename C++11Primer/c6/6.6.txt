A local variable is a variable which is defined within the scope of the function and is destroyed when
the function returns.

A parameter is a local variable which is initialized when the function is called

A static local varible is a variable which is within the scope of the function but has the same lifetime as the 
program, and retains its value after the function returns.
