a) the program falls through to all cases, because there are no break statments

b) ix is assigned in one case and used in another. This is illegal

c) you can't use the comma operator for case statements

d) you can't use variables in case statements.
