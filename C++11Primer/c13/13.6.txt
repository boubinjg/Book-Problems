A copy assign operator is an overloaded assignment operator which takes a reference to an object of the same class
and returns *this. The copy assign operator is used during assignment. 
A synthesized copy assignment assigns each RHS member value to LHS. it is synthesized when no copy assign is defined.
