If the loop used a vector, it would undergo a linear time operation to move the elements of the vector
backwards instead of just organizing a few pointers like it does with a list. This operation could invalidate
the iterator, causing the program to break
