This algorithm is sort of like heap sort, in that you can add elements to the binary tree, maintaining the
tree property. Then you can remove them, (or simply print them) in sorted order based on that property. This 
algorithm has a best/average case of O(nlog(n)), and a worst case of O(n^2). 
